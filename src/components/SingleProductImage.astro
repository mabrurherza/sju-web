---
import { Image } from "astro:assets";
const { source, alt } = Astro.props;
---

<img
  src={source[0]}
  alt={`Preview product of ${alt}`}
  class="w-full rounded-xl border border-blue-200 h-full"
  loading="lazy"
/>
